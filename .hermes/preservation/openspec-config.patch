diff --git a/openspec/config.yaml b/openspec/config.yaml
index eb3f147d..0da9b6ea 100644
--- a/openspec/config.yaml
+++ b/openspec/config.yaml
@@ -1,5 +1,6 @@
 # Kadarn Platform — OpenSpec SDD Configuration
 # Auto-generated by el Gentleman preflight (2026-06-27)
+# Updated by sdd-init (2026-07-18) — derived context refresh
 ---
 version: 1
 project: kadarn-platform
@@ -21,3 +22,62 @@ strict_tdd: false
 
 # Default test command
 test_command: npm test
+
+# ── Derived context (auto-refreshed by sdd-init) ──────────────────────
+
+# Test runner
+test_runner: vitest
+test_runner_version: 3.1.0
+e2e_runner: playwright
+e2e_runner_version: 1.61.1
+
+# Test commands available
+test_commands:
+  unit: "npm test"
+  typecheck: "npm run typecheck"
+  instrumentation: "npm run test:instrumentation"
+  coverage: "npm run test:coverage -w tests"
+  e2e: "npm run test:e2e -w apps/web"
+  e2e_ui: "npm run test:e2e:ui -w apps/web"
+  gate_web: "npm run gate:web -w apps/web"
+
+# Coverage thresholds (v8 provider)
+coverage_thresholds:
+  statements: 95
+  branches: 95
+  functions: 95
+  lines: 95
+
+# Stack snapshot
+stack:
+  language: typescript
+  language_version: 5.8-5.9
+  runtime: node
+  runtime_version: ">=20"
+  frontend: nextjs-16-react-19
+  styling: tailwind-4-shadcn-ui
+  database: postgresql-17-supabase
+  auth: supabase-auth-oidc
+  package_manager: npm-workspaces
+  module_system: esm
+  ts_target: ES2022
+  ts_strict: true
+
+# Workspace layout
+workspaces:
+  - apps/*
+  - packages/*
+  - tests
+
+# Key packages (34 total)
+package_count: 34
+apps:
+  - "@kadarn/api (apps/api, Next.js 16, port 3001)"
+  - "@kadarn/web (apps/web, Next.js 16, port 3000)"
+
+# CI pipelines
+ci_pipelines:
+  - ci.yml (typecheck + instrumentation + phase8 tests)
+  - security.yml
+  - architecture.yml
+  - performance.yml
