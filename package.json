{
  "name": "kadarn-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "tests"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w apps/web",
    "lint": "npm run lint -w apps/web",
    "test": "npm run test -w tests",
    "typecheck": "node node_modules/typescript/bin/tsc -p packages/types/tsconfig.json --noEmit && node node_modules/typescript/bin/tsc -p packages/instrumentation/tsconfig.json --noEmit && node node_modules/typescript/bin/tsc -p apps/api/tsconfig.json --noEmit",
    "audit:deps": "npm audit --audit-level=high",
    "test:instrumentation": "npm run test -w @kadarn/instrumentation",
    "sbom:generate": "bash scripts/generate-sbom.sh",
    "arch:gate": "bash scripts/arch-gate.sh",
    "check:secrets": "bash scripts/check-secrets.sh"
  }
}
