# Kadarn Platform — OpenSpec SDD Configuration
# Auto-generated by el Gentleman preflight (2026-06-27)
---
version: 1
project: kadarn-platform

# Execution mode: interactive | auto
mode: interactive

# Artifact storage: openspec | engram | both
artifact_store: openspec

# Chained PR strategy: auto-forecast | ask-always | single-pr-default | force-chained
chained_pr_strategy: auto-forecast

# Maximum changed lines per PR before splitting is recommended
review_budget_lines: 400

# Strict TDD mode — requires test evidence before/after changes
strict_tdd: false

# Default test command
test_command: npm test
