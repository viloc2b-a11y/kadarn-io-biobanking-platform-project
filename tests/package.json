{
  "name": "kadarn-security-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Kadarn Trust & Security Foundation — integration tests with real JWTs",
  "type": "module",
  "scripts": {
    "test": "vitest run policy provenance events financial fulfillment graph integration intelligence knowledge matching trust twins web workflow",
    "test:all": "vitest run",
    "test:integration": "vitest run api security",
    "test:watch": "vitest",
    "test:identity": "vitest run tests/security/identity.test.ts",
    "test:authorization": "vitest run tests/security/authorization.test.ts",
    "test:audit": "vitest run tests/security/audit.test.ts",
    "test:threat": "vitest run tests/security/threat.test.ts",
    "test:security": "vitest run tests/security/",
    "test:phase8": "vitest run phase8",
    "test:gate-28jk": "vitest run phase8/legacy-equivalence/gate.test.ts",
    "staging:cutover-smoke": "npx tsx phase8/staging-cutover-smoke.ts",
    "test:instrumentation": "vitest run instrumentation",
    "test:performance": "vitest run performance",
    "test:coverage": "vitest run --coverage",
    "seed:users": "npx tsx setup/seed-users.ts"
  },
  "devDependencies": {
    "@kadarn/published-view": "*",
    "@supabase/supabase-js": "^2.49.0",
    "@types/node": "^26.0.1",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
