{
  "name": "@kadarn/document-intake",
  "version": "0.1.0",
  "description": "Kadarn Document Intake Engine — Document normalization pipeline that consumes format-specific adapters before Discovery. Infrastructure layer.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {}
}
