{
  "name": "@kadarn/platform-services",
  "version": "0.2.0",
  "description": "Kadarn Platform Services — shared infrastructure for all engines",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@kadarn/domain-events": "../../packages/domain-events"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
